/node_modules
.env
.env.local
.next
.next/*
.claude/

# Database dumps and local secrets — never commit these
kondo_backup.sql
reset.sql
secrets.txt
*.dump
.DS_Store
tsconfig.tsbuildinfo

# Local seed/SQL working area — do not commit
temp-sql/
